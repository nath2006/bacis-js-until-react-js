// Demo perulangan  menggunakan for loop

/*
    KETIGA STATMENT INI ADALAH EQUIVALENT
    hitung=hitung+1;
    hitung+=1;
    hitung++
*/

let y = 10;

for(let hitung=0; hitung<y; hitung++){
    console.log(`Putar ke-${hitung}`);
}